apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-html-data
  namespace: other-project-workloads
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi