apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  namespace: other-project-workload
spec:
  selector:
    app: uptime-kuma
  type: NodePort
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30081