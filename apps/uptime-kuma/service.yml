apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: uptime-kuma:/Service:uptime-kuma/uptime-kuma
  name: uptime-kuma
  namespace: uptime-kuma
spec:
  ports:
    - port: 3001
      protocol: TCP
      targetPort: 3001
  selector:
    app: uptime-kuma
  type: ClusterIP
